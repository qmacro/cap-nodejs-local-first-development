{
  "name": "@qmacro/emitter",
  "version": "1.0.0",
  "description": "Emitter",
  "dependencies": {
    "@sap/cds": "^9"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2"
  },
  "cds": {
    "requires": {
      "db": {
        "driver": "node"
      },
      "messaging": {
        "kind": "file-based-messaging"
      }
    }
  }
}
